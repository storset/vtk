
getDescription() {
    return "Displays repository ID";
}

getUsage() {
    return "repo id";
}

void execute(context, args, out) {
    out.println(context.get("context").getBean("repository").getId());
}

