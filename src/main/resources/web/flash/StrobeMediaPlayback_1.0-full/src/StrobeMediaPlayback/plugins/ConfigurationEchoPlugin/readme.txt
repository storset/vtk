ConfigurationEchoPlugin is used for testing the plugin configuration API that is implemented in StrobeMediaPlayer.

It will display a javascript alert dialog if alert metadata is set. 
Otherwise it will set the resource metadata as asset metadata so that it can be check inside unit tests.